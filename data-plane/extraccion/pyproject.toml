[project]
name = "quilate-extraccion"
version = "0.1.0"
description = "Extracción read-only del ERP (SAP Business One / HANA, Odoo) hacia Bronce (Postgres). Motor agnóstico de Quilate Analytics: la organización se resuelve en tiempo de ejecución."
requires-python = ">=3.10"
dependencies = [
  "hdbcli>=2.19",           # driver oficial SAP HANA (read-only)
  "pymssql>=2.2",           # SAP B1 sobre SQL Server (read-only)
  "psycopg[binary]>=3.1",   # escritura a Bronze (Postgres)
  "pydantic>=2.6",
  "pydantic-settings>=2.2", # config desde .env (equivalente a Zod para entradas externas)
  "structlog>=24.1",        # logging estructurado (sin secretos)
  "fastapi>=0.110",         # worker HTTP que el portal dispara (introspección/extracción)
  "uvicorn[standard]>=0.29",
  "dbt-postgres>=1.7",      # transformación Bronze→Silver→Gold disparada desde el portal (dbtRunner)
]

[project.optional-dependencies]
dev = ["ruff>=0.3", "mypy>=1.9", "pytest>=8.0"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
strict = true
python_version = "3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
