{
  "name": "@pulso/agente",
  "version": "0.1.0",
  "description": "Agente de IA gobernado: tools tipadas + guardas + loop, sin acceso directo a BD (recibe el ejecutor inyectado)",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
